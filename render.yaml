services:
  - type: web
    name: mbs-financial-calculators
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 server.py"
    envVars:
      - key: PYTHONUNBUFFERED
        value: "true"
